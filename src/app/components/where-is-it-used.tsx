'use server'

import { Box } from "@mui/material";

export default async function WhereIsItUsed() {
  return (
    <Box sx={{ height: '590px', bgcolor: "#fff" }}>
      Where is it used
    </Box>
  )
}
