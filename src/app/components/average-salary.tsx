'use server'

import { Box } from "@mui/material";

export default async function AverageSalary() {
  return (
    <Box sx={{ height: '596px', bgcolor: "#fff" }}>
      Average salary
    </Box>
  )
}
