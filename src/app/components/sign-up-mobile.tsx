'use server'

import { Box } from "@mui/material";

export default async function SignUpMobile() {
  return (
    <Box sx={{ height: '375px', bgcolor: "#fff" }}>
      sign-up-mobile
    </Box>
  )
}
