'use server'

import { Box } from "@mui/material";

export default async function DeveloperSignUp() {
  return (
    <Box sx={{ height: '813px', bgcolor: "#fff" }}>
      C# - разработчик
    </Box>
  )
}
